import React from 'react';
import './Header.css'

const Header = () => {
    return (
        <nav>
            <div>
                <h2>this is header</h2>
            </div>
        </nav>
    );
};

export default Header;